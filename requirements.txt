# NOTE: numpy must be listed first because some packages below (like lap)
# require numpy to be available during their build process.
# Keep numpy at the top of this file to ensure proper installation order.
numpy>=1.21.0
openvino-dev 
nncf
opencv-contrib-python>=4.8.1.78
onnxruntime>=1.16.0
# Optional: onnxruntime-training enables backpropagation in OnlineTraining node
# Install with: pip install onnxruntime-training
# Optional: torch + onnx2torch enable REAL network backprop (backbone/heads)
# for the OnlineTraining student. When absent, the node falls back to the
# built-in affine correction head. Install with: pip install torch onnx2torch
onnx>=1.14.0
torch>=2.0.0
transformers>=4.30.0
dearpygui>=2.0.0
mediapipe>=0.10.0
protobuf>=3.20.2,<4.0.0
filterpy>=1.4.5
scipy>=1.10.0
pafy>=0.5.5
ffmpeg-python>=0.2.0
imageio-ffmpeg>=0.4.8
pytest>=7.0.0
# Note: youtube-dl is kept for pafy backward compatibility
# but yt-dlp is preferred and actively maintained
youtube-dl>=2021.12.17
yt-dlp>=2023.3.4
pymongo>=4.0.0
dnspython>=2.0.0
pytz>=2021.3
Pillow>=9.0.0
librosa>=0.10.0
matplotlib>=3.8,<3.9
soundfile>=0.12.0
sounddevice>=0.4.6
requests>=2.28.0
scikit-learn==1.4.2
lap>=0.5.0
motpy>=0.0.10
norfair>=2.2.0
rich>=10.0.0
pyserial>=3.5
streamlink>=6.0.0
websockets>=11.0.0
contextily>=1.3.0
python-dotenv>=0.19.0
wordcloud>=1.9.0
onvif-zeep>=0.2.12
WSDiscovery>=2.0.0
psutil>=5.9.0
# Text-to-speech: piper is the default vocalizer for the Text2Speech node
# Install piper manually from: https://github.com/rhasspy/piper/releases
# piper-tts (Python wrapper, optional)
piper-tts>=1.2.0
