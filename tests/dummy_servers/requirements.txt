# Optional dependencies for dummy test servers
# Install with: pip install -r tests/dummy_servers/requirements.txt

# Required for all servers
numpy>=1.19.0
Pillow>=8.0.0

# Required for WebSocket server
websockets>=10.0

# Required for WebRTC server (optional)
aiohttp>=3.8.0
aiortc>=1.3.0

# For testing
pytest>=7.0.0
